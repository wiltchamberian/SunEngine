#version 430 core
out vec4 FragColor;

in vec2 TexCoord;
uniform sampler2D img_output;
//uniform image2D img_output;

void main()
{
   FragColor = texture(img_output, TexCoord);
   //FragColor =  texture(img_output, vec2(gl_FragCoord.xy) / vec2(textureSize(img_output, 0)));
   //FragColor = texture(img_output, vec2(gl_FragCoord.xy)/512);
   //FragColor = vec4(0.2,0.2,1,1);
};